// Numerical utilities shared across indicators and risk engines.
export function toDecimal(value: string | number, precision = 18): string {
  // TODO: replace with big number implementation.
  return value.toString();
}
