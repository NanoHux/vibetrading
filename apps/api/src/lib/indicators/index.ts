// Indicator calculation helpers (EMA, MACD, RSI, ATR) will live here.
export function calculateEma(values: number[], period: number): number[] {
  // TODO: implement EMA with proper smoothing.
  return values;
}
